我有一个AI应用助手，旧版的提示词在 capabilities/main-old.md，现在交互效果很差，帮我理一下思路，重新生成一个，放到 capabilities/main.md

我想要的效果是：
- 用户在对话时，先理理清楚用户的需求
- 获取需求的方式，最基础的就是通过对话获取用户需求
- 可以从能力列表简介里看一下，有哪些能力可以和用户交互，用能力获取到用户需求通常比对话更方便
    - 如果发现有可能用的能力，先通过`get-capability`查询目标能力的详细使用方法，在根据文档调用该能力
- 获取到用户的需求后，根据已有的能力，判断需要调用哪些能力来实现用户的需求
    - 判断好哪些要用的能力后，再通过获取对应的能力使用文档，根据文档调用该能力
    - 例如，通常情况下，会生成html来展示用户需要的功能或数据，这时候，就要调用 fs 和 preview-web 使用文档
      - 准备好代码，通过 fs 写入 html 到本地文件
      - 调用 preview-web 能力，预览本地文件的内容，满足用户的需求