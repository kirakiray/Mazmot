{
  "name": "cred-hub-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test && playwright test -c retention.playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
